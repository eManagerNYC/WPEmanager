-- ============================================================
-- eManager — MySQL reference schema
-- ------------------------------------------------------------
-- FOR REFERENCE ONLY. eManager creates and updates these tables
-- automatically (via dbDelta) on activation and whenever a module
-- is installed; see includes/class-em-db.php. Table names below
-- omit the WordPress table prefix (e.g. wp_).
-- Generated by tools/generate-modules.js.
-- ============================================================

CREATE TABLE em_companies (
  id bigint(20) unsigned NOT NULL auto_increment,
  name varchar(191) DEFAULT NULL,
  type varchar(191) DEFAULT NULL,
  phone varchar(191) DEFAULT NULL,
  email varchar(191) DEFAULT NULL,
  address text DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY name (name)
);

CREATE TABLE em_comments (
  id bigint(20) unsigned NOT NULL auto_increment,
  module_id varchar(191) DEFAULT NULL,
  record_id bigint(20) unsigned DEFAULT NULL,
  body text DEFAULT NULL,
  author_id varchar(20) DEFAULT NULL,
  author_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY record (module_id,record_id)
);

CREATE TABLE em_activity (
  id bigint(20) unsigned NOT NULL auto_increment,
  module_id varchar(191) DEFAULT NULL,
  record_id bigint(20) unsigned DEFAULT NULL,
  actor_id varchar(20) DEFAULT NULL,
  actor_name varchar(191) DEFAULT NULL,
  action varchar(40) DEFAULT NULL,
  from_status varchar(191) DEFAULT NULL,
  to_status varchar(191) DEFAULT NULL,
  note text DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY record (module_id,record_id)
);

-- Qualified Bidders (preconstruction)
CREATE TABLE em_qualified_bidders (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `company_name` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `contact_name` varchar(191) DEFAULT NULL,
  `email` varchar(191) DEFAULT NULL,
  `phone` varchar(191) DEFAULT NULL,
  `emr` decimal(20,4) DEFAULT NULL,
  `bonding_capacity` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_company_name` (`company_name`),
  KEY `idx_trade` (`trade`),
  KEY `idx_contact_name` (`contact_name`),
  KEY `idx_bonding_capacity` (`bonding_capacity`)
);

-- Bid Packages (preconstruction)
CREATE TABLE em_bid_packages (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `number` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `csi_division` varchar(191) DEFAULT NULL,
  `bid_due` date DEFAULT NULL,
  `estimate` decimal(20,4) DEFAULT NULL,
  `scope` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_number` (`number`),
  KEY `idx_title` (`title`),
  KEY `idx_bid_due` (`bid_due`),
  KEY `idx_estimate` (`estimate`)
);

-- Bid Manual (preconstruction)
CREATE TABLE em_bid_manual (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `section_no` varchar(191) DEFAULT NULL,
  `revision` varchar(191) DEFAULT NULL,
  `issue_date` date DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  `summary` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_section_no` (`section_no`),
  KEY `idx_revision` (`revision`),
  KEY `idx_issue_date` (`issue_date`)
);

-- RFIs (engineering)
CREATE TABLE em_rfis (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `number` varchar(191) DEFAULT NULL,
  `subject` varchar(191) DEFAULT NULL,
  `discipline` varchar(191) DEFAULT NULL,
  `priority` varchar(191) DEFAULT NULL,
  `question` text DEFAULT NULL,
  `ball_in_court` varchar(191) DEFAULT NULL,
  `assigned_to` varchar(191) DEFAULT NULL,
  `date_submitted` date DEFAULT NULL,
  `date_required` date DEFAULT NULL,
  `drawing_ref` varchar(191) DEFAULT NULL,
  `answer` text DEFAULT NULL,
  `responded_by` varchar(191) DEFAULT NULL,
  `response_date` date DEFAULT NULL,
  `cost_impact` tinyint(1) DEFAULT NULL,
  `cost_impact_amount` decimal(20,4) DEFAULT NULL,
  `schedule_impact` tinyint(1) DEFAULT NULL,
  `schedule_impact_days` decimal(20,4) DEFAULT NULL,
  `distribution` text DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_number` (`number`),
  KEY `idx_subject` (`subject`),
  KEY `idx_discipline` (`discipline`),
  KEY `idx_priority` (`priority`),
  KEY `idx_ball_in_court` (`ball_in_court`),
  KEY `idx_date_submitted` (`date_submitted`),
  KEY `idx_date_required` (`date_required`)
);

-- Submittals (engineering)
CREATE TABLE em_submittals (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `number` varchar(191) DEFAULT NULL,
  `revision` varchar(191) DEFAULT NULL,
  `submittal_type` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `spec_section` varchar(191) DEFAULT NULL,
  `subcontractor` varchar(191) DEFAULT NULL,
  `ball_in_court` varchar(191) DEFAULT NULL,
  `reviewer` varchar(191) DEFAULT NULL,
  `lead_time_days` decimal(20,4) DEFAULT NULL,
  `required_on_site` date DEFAULT NULL,
  `date_received` date DEFAULT NULL,
  `date_returned` date DEFAULT NULL,
  `reviewer_notes` text DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_number` (`number`),
  KEY `idx_revision` (`revision`),
  KEY `idx_submittal_type` (`submittal_type`),
  KEY `idx_title` (`title`),
  KEY `idx_spec_section` (`spec_section`),
  KEY `idx_subcontractor` (`subcontractor`),
  KEY `idx_ball_in_court` (`ball_in_court`),
  KEY `idx_required_on_site` (`required_on_site`),
  KEY `idx_date_returned` (`date_returned`)
);

-- Drawings (engineering)
CREATE TABLE em_drawings (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `number` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `discipline` varchar(191) DEFAULT NULL,
  `set_type` varchar(191) DEFAULT NULL,
  `revision` varchar(191) DEFAULT NULL,
  `rev_date` date DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_number` (`number`),
  KEY `idx_title` (`title`),
  KEY `idx_discipline` (`discipline`),
  KEY `idx_set_type` (`set_type`),
  KEY `idx_revision` (`revision`),
  KEY `idx_rev_date` (`rev_date`)
);

-- Specifications (engineering)
CREATE TABLE em_specifications (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `section_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `division` varchar(191) DEFAULT NULL,
  `revision` varchar(191) DEFAULT NULL,
  `issue_date` date DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_section_no` (`section_no`),
  KEY `idx_title` (`title`),
  KEY `idx_division` (`division`),
  KEY `idx_revision` (`revision`),
  KEY `idx_issue_date` (`issue_date`)
);

-- File Explorer (engineering)
CREATE TABLE em_file_explorer (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `name` varchar(191) DEFAULT NULL,
  `folder` varchar(191) DEFAULT NULL,
  `category` varchar(191) DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  `size_kb` decimal(20,4) DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_name` (`name`),
  KEY `idx_folder` (`folder`),
  KEY `idx_category` (`category`)
);

-- Permitting (engineering)
CREATE TABLE em_permitting (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `permit_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `permit_type` varchar(191) DEFAULT NULL,
  `authority` varchar(191) DEFAULT NULL,
  `applied_date` date DEFAULT NULL,
  `issued_date` date DEFAULT NULL,
  `expires` date DEFAULT NULL,
  `fee` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_permit_no` (`permit_no`),
  KEY `idx_title` (`title`),
  KEY `idx_permit_type` (`permit_type`),
  KEY `idx_authority` (`authority`),
  KEY `idx_issued_date` (`issued_date`),
  KEY `idx_expires` (`expires`)
);

-- Meeting Agenda / Minutes (engineering)
CREATE TABLE em_meetings (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `meeting_no` varchar(191) DEFAULT NULL,
  `meeting_type` varchar(191) DEFAULT NULL,
  `meeting_date` datetime DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `attendees` text DEFAULT NULL,
  `agenda` text DEFAULT NULL,
  `minutes` text DEFAULT NULL,
  `next_meeting` datetime DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_meeting_no` (`meeting_no`),
  KEY `idx_meeting_type` (`meeting_type`),
  KEY `idx_meeting_date` (`meeting_date`)
);

-- Transmittals (engineering)
CREATE TABLE em_transmittals (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `number` varchar(191) DEFAULT NULL,
  `to_company` varchar(191) DEFAULT NULL,
  `attention` varchar(191) DEFAULT NULL,
  `purpose` varchar(191) DEFAULT NULL,
  `sent_date` date DEFAULT NULL,
  `via` varchar(191) DEFAULT NULL,
  `contents` text DEFAULT NULL,
  `remarks` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_number` (`number`),
  KEY `idx_to_company` (`to_company`),
  KEY `idx_purpose` (`purpose`),
  KEY `idx_sent_date` (`sent_date`),
  KEY `idx_via` (`via`)
);

-- Daily Reports (field)
CREATE TABLE em_daily_reports (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `report_date` date DEFAULT NULL,
  `superintendent` varchar(191) DEFAULT NULL,
  `weather` varchar(191) DEFAULT NULL,
  `temp_high` decimal(20,4) DEFAULT NULL,
  `temp_low` decimal(20,4) DEFAULT NULL,
  `precipitation` decimal(20,4) DEFAULT NULL,
  `manpower` decimal(20,4) DEFAULT NULL,
  `work_performed` text DEFAULT NULL,
  `deliveries` text DEFAULT NULL,
  `equipment_on_site` text DEFAULT NULL,
  `delays` text DEFAULT NULL,
  `safety_notes` text DEFAULT NULL,
  `visitors` text DEFAULT NULL,
  `superintendent_signature` mediumtext DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_report_date` (`report_date`),
  KEY `idx_superintendent` (`superintendent`),
  KEY `idx_weather` (`weather`),
  KEY `idx_manpower` (`manpower`)
);

-- Photo Library (field)
CREATE TABLE em_photo_library (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `photo_url` varchar(191) DEFAULT NULL,
  `album` varchar(191) DEFAULT NULL,
  `taken_on` date DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `tags` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_album` (`album`),
  KEY `idx_taken_on` (`taken_on`),
  KEY `idx_location` (`location`),
  KEY `idx_trade` (`trade`)
);

-- Schedule (field)
CREATE TABLE em_schedule (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `activity_id` varchar(191) DEFAULT NULL,
  `activity` varchar(191) DEFAULT NULL,
  `start_date` date DEFAULT NULL,
  `finish_date` date DEFAULT NULL,
  `percent_complete` decimal(20,4) DEFAULT NULL,
  `responsible` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_activity_id` (`activity_id`),
  KEY `idx_activity` (`activity`),
  KEY `idx_start_date` (`start_date`),
  KEY `idx_finish_date` (`finish_date`),
  KEY `idx_percent_complete` (`percent_complete`)
);

-- Checklists (field)
CREATE TABLE em_checklists (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `checklist_type` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `responsible` varchar(191) DEFAULT NULL,
  `due_date` date DEFAULT NULL,
  `items` text DEFAULT NULL,
  `results` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_checklist_type` (`checklist_type`),
  KEY `idx_location` (`location`),
  KEY `idx_responsible` (`responsible`),
  KEY `idx_due_date` (`due_date`)
);

-- Punch List (field)
CREATE TABLE em_punchlist (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `item_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `responsible` varchar(191) DEFAULT NULL,
  `ball_in_court` varchar(191) DEFAULT NULL,
  `priority` varchar(191) DEFAULT NULL,
  `due_date` date DEFAULT NULL,
  `backcharge` decimal(20,4) DEFAULT NULL,
  `photo_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_item_no` (`item_no`),
  KEY `idx_title` (`title`),
  KEY `idx_location` (`location`),
  KEY `idx_responsible` (`responsible`),
  KEY `idx_ball_in_court` (`ball_in_court`),
  KEY `idx_priority` (`priority`),
  KEY `idx_due_date` (`due_date`)
);

-- Pull Planning (field)
CREATE TABLE em_pull_planning (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `milestone` varchar(191) DEFAULT NULL,
  `task` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `promise_date` date DEFAULT NULL,
  `duration_days` decimal(20,4) DEFAULT NULL,
  `constraints` text DEFAULT NULL,
  `variance_reason` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_milestone` (`milestone`),
  KEY `idx_task` (`task`),
  KEY `idx_trade` (`trade`),
  KEY `idx_promise_date` (`promise_date`)
);

-- Observations (safety)
CREATE TABLE em_observations (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `observation_type` varchar(191) DEFAULT NULL,
  `severity` varchar(191) DEFAULT NULL,
  `observed_on` date DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `company_observed` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `corrective_action` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_observation_type` (`observation_type`),
  KEY `idx_severity` (`severity`),
  KEY `idx_observed_on` (`observed_on`),
  KEY `idx_company_observed` (`company_observed`)
);

-- Pre-Task Plans (PTPs) (safety)
CREATE TABLE em_pretask_plans (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `task` varchar(191) DEFAULT NULL,
  `plan_date` date DEFAULT NULL,
  `crew` varchar(191) DEFAULT NULL,
  `crew_size` decimal(20,4) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `hazards` text DEFAULT NULL,
  `controls` text DEFAULT NULL,
  `tools` text DEFAULT NULL,
  `foreman_signature` mediumtext DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_task` (`task`),
  KEY `idx_plan_date` (`plan_date`),
  KEY `idx_crew` (`crew`),
  KEY `idx_crew_size` (`crew_size`)
);

-- Job Hazard Analyses (JHAs) (safety)
CREATE TABLE em_jhas (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `revision` varchar(191) DEFAULT NULL,
  `review_date` date DEFAULT NULL,
  `job_steps` text DEFAULT NULL,
  `hazards` text DEFAULT NULL,
  `controls` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_trade` (`trade`),
  KEY `idx_revision` (`revision`),
  KEY `idx_review_date` (`review_date`)
);

-- Employee Orientations (safety)
CREATE TABLE em_orientations (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `employee_name` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `orientation_date` date DEFAULT NULL,
  `badge_no` varchar(191) DEFAULT NULL,
  `emergency_contact` varchar(191) DEFAULT NULL,
  `certifications` text DEFAULT NULL,
  `employee_signature` mediumtext DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_employee_name` (`employee_name`),
  KEY `idx_company` (`company`),
  KEY `idx_trade` (`trade`),
  KEY `idx_orientation_date` (`orientation_date`),
  KEY `idx_badge_no` (`badge_no`)
);

-- Prime Contract (contracts)
CREATE TABLE em_prime_contract (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `contract_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `contract_type` varchar(191) DEFAULT NULL,
  `owner` varchar(191) DEFAULT NULL,
  `original_value` decimal(20,4) DEFAULT NULL,
  `executed_date` date DEFAULT NULL,
  `retainage_pct` decimal(20,4) DEFAULT NULL,
  `scope` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_contract_no` (`contract_no`),
  KEY `idx_title` (`title`),
  KEY `idx_contract_type` (`contract_type`),
  KEY `idx_owner` (`owner`),
  KEY `idx_original_value` (`original_value`)
);

-- Subcontracts (contracts)
CREATE TABLE em_subcontracts (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `subcontract_no` varchar(191) DEFAULT NULL,
  `subcontractor` varchar(191) DEFAULT NULL,
  `csi_division` varchar(191) DEFAULT NULL,
  `scope_title` varchar(191) DEFAULT NULL,
  `value` decimal(20,4) DEFAULT NULL,
  `executed_date` date DEFAULT NULL,
  `retainage_pct` decimal(20,4) DEFAULT NULL,
  `inclusions` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_subcontract_no` (`subcontract_no`),
  KEY `idx_subcontractor` (`subcontractor`),
  KEY `idx_scope_title` (`scope_title`),
  KEY `idx_value` (`value`),
  KEY `idx_executed_date` (`executed_date`)
);

-- Professional Service Agreements (contracts)
CREATE TABLE em_psas (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `agreement_no` varchar(191) DEFAULT NULL,
  `consultant` varchar(191) DEFAULT NULL,
  `service` varchar(191) DEFAULT NULL,
  `value` decimal(20,4) DEFAULT NULL,
  `start_date` date DEFAULT NULL,
  `end_date` date DEFAULT NULL,
  `terms` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_agreement_no` (`agreement_no`),
  KEY `idx_consultant` (`consultant`),
  KEY `idx_service` (`service`),
  KEY `idx_value` (`value`),
  KEY `idx_end_date` (`end_date`)
);

-- Lien Waivers (contracts)
CREATE TABLE em_lien_waivers (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `waiver_type` varchar(191) DEFAULT NULL,
  `through_date` date DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `received_date` date DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  `authorized_signature` mediumtext DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_company` (`company`),
  KEY `idx_waiver_type` (`waiver_type`),
  KEY `idx_through_date` (`through_date`),
  KEY `idx_amount` (`amount`),
  KEY `idx_received_date` (`received_date`)
);

-- Certificates of Insurance (contracts)
CREATE TABLE em_insurance_certs (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `carrier` varchar(191) DEFAULT NULL,
  `policy_no` varchar(191) DEFAULT NULL,
  `coverage_type` varchar(191) DEFAULT NULL,
  `effective_date` date DEFAULT NULL,
  `expiration_date` date DEFAULT NULL,
  `limit` decimal(20,4) DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_company` (`company`),
  KEY `idx_carrier` (`carrier`),
  KEY `idx_policy_no` (`policy_no`),
  KEY `idx_coverage_type` (`coverage_type`),
  KEY `idx_expiration_date` (`expiration_date`)
);

-- Letters of Intent (contracts)
CREATE TABLE em_letters_of_intent (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `loi_no` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `scope` varchar(191) DEFAULT NULL,
  `not_to_exceed` decimal(20,4) DEFAULT NULL,
  `issued_date` date DEFAULT NULL,
  `expiration_date` date DEFAULT NULL,
  `terms` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_loi_no` (`loi_no`),
  KEY `idx_company` (`company`),
  KEY `idx_scope` (`scope`),
  KEY `idx_not_to_exceed` (`not_to_exceed`),
  KEY `idx_issued_date` (`issued_date`)
);

-- Budget & Forecast (cost)
CREATE TABLE em_budget_forecast (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `cost_code` varchar(191) DEFAULT NULL,
  `description` varchar(191) DEFAULT NULL,
  `original_budget` decimal(20,4) DEFAULT NULL,
  `approved_changes` decimal(20,4) DEFAULT NULL,
  `committed` decimal(20,4) DEFAULT NULL,
  `forecast_final` decimal(20,4) DEFAULT NULL,
  `variance_notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_cost_code` (`cost_code`),
  KEY `idx_description` (`description`),
  KEY `idx_original_budget` (`original_budget`),
  KEY `idx_committed` (`committed`),
  KEY `idx_forecast_final` (`forecast_final`)
);

-- Invoicing (AIA G702/G703) (cost)
CREATE TABLE em_invoicing (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `application_no` varchar(191) DEFAULT NULL,
  `period_to` date DEFAULT NULL,
  `scheduled_value` decimal(20,4) DEFAULT NULL,
  `work_completed` decimal(20,4) DEFAULT NULL,
  `stored_materials` decimal(20,4) DEFAULT NULL,
  `retainage` decimal(20,4) DEFAULT NULL,
  `current_due` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_application_no` (`application_no`),
  KEY `idx_period_to` (`period_to`),
  KEY `idx_work_completed` (`work_completed`),
  KEY `idx_current_due` (`current_due`)
);

-- Direct Costs (cost)
CREATE TABLE em_direct_costs (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `cost_type` varchar(191) DEFAULT NULL,
  `vendor` varchar(191) DEFAULT NULL,
  `reference_no` varchar(191) DEFAULT NULL,
  `cost_code` varchar(191) DEFAULT NULL,
  `cost_date` date DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_cost_type` (`cost_type`),
  KEY `idx_vendor` (`vendor`),
  KEY `idx_reference_no` (`reference_no`),
  KEY `idx_cost_code` (`cost_code`),
  KEY `idx_cost_date` (`cost_date`),
  KEY `idx_amount` (`amount`)
);

-- Potential Changes (cost)
CREATE TABLE em_potential_changes (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `pco_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `reason` varchar(191) DEFAULT NULL,
  `rom_estimate` decimal(20,4) DEFAULT NULL,
  `cost_code` varchar(191) DEFAULT NULL,
  `identified_date` date DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_pco_no` (`pco_no`),
  KEY `idx_title` (`title`),
  KEY `idx_reason` (`reason`),
  KEY `idx_rom_estimate` (`rom_estimate`),
  KEY `idx_cost_code` (`cost_code`),
  KEY `idx_identified_date` (`identified_date`)
);

-- Change Orders (cost)
CREATE TABLE em_change_orders (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `co_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `co_type` varchar(191) DEFAULT NULL,
  `cost_code` varchar(191) DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `time_extension_days` decimal(20,4) DEFAULT NULL,
  `executed_date` date DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_co_no` (`co_no`),
  KEY `idx_title` (`title`),
  KEY `idx_co_type` (`co_type`),
  KEY `idx_cost_code` (`cost_code`),
  KEY `idx_amount` (`amount`),
  KEY `idx_executed_date` (`executed_date`)
);

-- Approval Letters & Directives (cost)
CREATE TABLE em_approval_letters (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `letter_no` varchar(191) DEFAULT NULL,
  `letter_type` varchar(191) DEFAULT NULL,
  `subject` varchar(191) DEFAULT NULL,
  `issued_to` varchar(191) DEFAULT NULL,
  `issued_date` date DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `body` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_letter_no` (`letter_no`),
  KEY `idx_letter_type` (`letter_type`),
  KEY `idx_subject` (`subject`),
  KEY `idx_issued_to` (`issued_to`),
  KEY `idx_issued_date` (`issued_date`)
);

-- Time & Materials Tickets (cost)
CREATE TABLE em_tm_tickets (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `ticket_no` varchar(191) DEFAULT NULL,
  `work_date` date DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `labor_hours` decimal(20,4) DEFAULT NULL,
  `material_cost` decimal(20,4) DEFAULT NULL,
  `equipment_cost` decimal(20,4) DEFAULT NULL,
  `total` decimal(20,4) DEFAULT NULL,
  `authorized_signature` mediumtext DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_ticket_no` (`ticket_no`),
  KEY `idx_work_date` (`work_date`),
  KEY `idx_company` (`company`),
  KEY `idx_labor_hours` (`labor_hours`),
  KEY `idx_total` (`total`)
);

-- 3D Models (bim)
CREATE TABLE em_bim_models (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `discipline` varchar(191) DEFAULT NULL,
  `version` varchar(191) DEFAULT NULL,
  `model_date` date DEFAULT NULL,
  `ifc_url` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_discipline` (`discipline`),
  KEY `idx_version` (`version`),
  KEY `idx_model_date` (`model_date`)
);

-- Coordination Issues (bim)
CREATE TABLE em_coordination_issues (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `issue_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `clash_type` varchar(191) DEFAULT NULL,
  `models_involved` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `priority` varchar(191) DEFAULT NULL,
  `due_date` date DEFAULT NULL,
  `description` text DEFAULT NULL,
  `resolution` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_issue_no` (`issue_no`),
  KEY `idx_title` (`title`),
  KEY `idx_clash_type` (`clash_type`),
  KEY `idx_models_involved` (`models_involved`),
  KEY `idx_location` (`location`),
  KEY `idx_priority` (`priority`),
  KEY `idx_due_date` (`due_date`)
);

-- PCO Requests (PCOR) (change-management)
CREATE TABLE em_pco_requests (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `pcor_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `origin` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `drawing_ref` varchar(191) DEFAULT NULL,
  `photo_url` varchar(191) DEFAULT NULL,
  `rom_estimate` decimal(20,4) DEFAULT NULL,
  `cost_impact` tinyint(1) DEFAULT NULL,
  `schedule_impact` tinyint(1) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_pcor_no` (`pcor_no`),
  KEY `idx_title` (`title`),
  KEY `idx_origin` (`origin`),
  KEY `idx_rom_estimate` (`rom_estimate`)
);

-- Notifications of Change (NOC) (change-management)
CREATE TABLE em_nocs (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `noc_no` varchar(191) DEFAULT NULL,
  `pco_number` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `rom_estimate` decimal(20,4) DEFAULT NULL,
  `owner_comments` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_noc_no` (`noc_no`),
  KEY `idx_pco_number` (`pco_number`),
  KEY `idx_title` (`title`),
  KEY `idx_rom_estimate` (`rom_estimate`)
);

-- Supplemental Scope Info (SSI) (change-management)
CREATE TABLE em_ssi (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `ssi_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `scope_description` text DEFAULT NULL,
  `pco_number` varchar(191) DEFAULT NULL,
  `funding_source` varchar(191) DEFAULT NULL,
  `backup_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_ssi_no` (`ssi_no`),
  KEY `idx_title` (`title`),
  KEY `idx_pco_number` (`pco_number`)
);

-- Scope / PCO Directives (change-management)
CREATE TABLE em_directives (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `directive_no` varchar(191) DEFAULT NULL,
  `directive_type` varchar(191) DEFAULT NULL,
  `subcontractor` varchar(191) DEFAULT NULL,
  `pco_number` varchar(191) DEFAULT NULL,
  `scope` text DEFAULT NULL,
  `rom_estimate` decimal(20,4) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_directive_no` (`directive_no`),
  KEY `idx_directive_type` (`directive_type`),
  KEY `idx_subcontractor` (`subcontractor`),
  KEY `idx_pco_number` (`pco_number`)
);

-- Subcontractor Proposals (change-management)
CREATE TABLE em_proposals (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `proposal_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `subcontractor` varchar(191) DEFAULT NULL,
  `directive_no` varchar(191) DEFAULT NULL,
  `scope` text DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `breakdown` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_proposal_no` (`proposal_no`),
  KEY `idx_title` (`title`),
  KEY `idx_subcontractor` (`subcontractor`),
  KEY `idx_directive_no` (`directive_no`),
  KEY `idx_amount` (`amount`)
);

-- Change Order Requests (COR/AL) (change-management)
CREATE TABLE em_cors (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `cor_no` varchar(191) DEFAULT NULL,
  `letter_type` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `scope` text DEFAULT NULL,
  `owner_comments` text DEFAULT NULL,
  `cover_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_cor_no` (`cor_no`),
  KEY `idx_letter_type` (`letter_type`),
  KEY `idx_title` (`title`),
  KEY `idx_amount` (`amount`)
);

-- eTickets (T&M) (change-management)
CREATE TABLE em_etickets (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `ticket_no` varchar(191) DEFAULT NULL,
  `work_date` date DEFAULT NULL,
  `subcontractor` varchar(191) DEFAULT NULL,
  `directive_no` varchar(191) DEFAULT NULL,
  `responsible_person` varchar(191) DEFAULT NULL,
  `work_description` text DEFAULT NULL,
  `line_items` longtext DEFAULT NULL,
  `labor_total` decimal(20,4) DEFAULT NULL,
  `material_total` decimal(20,4) DEFAULT NULL,
  `equipment_total` decimal(20,4) DEFAULT NULL,
  `total` decimal(20,4) DEFAULT NULL,
  `super_signature` mediumtext DEFAULT NULL,
  `gc_signature` mediumtext DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_ticket_no` (`ticket_no`),
  KEY `idx_work_date` (`work_date`),
  KEY `idx_subcontractor` (`subcontractor`),
  KEY `idx_directive_no` (`directive_no`),
  KEY `idx_total` (`total`)
);

-- Daily Construction Reports (DCR) (change-management)
CREATE TABLE em_dcrs (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `dcr_no` varchar(191) DEFAULT NULL,
  `report_date` date DEFAULT NULL,
  `subcontractor` varchar(191) DEFAULT NULL,
  `work_performed` text DEFAULT NULL,
  `manpower` decimal(20,4) DEFAULT NULL,
  `equipment_used` text DEFAULT NULL,
  `delays` text DEFAULT NULL,
  `sub_signature` mediumtext DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_dcr_no` (`dcr_no`),
  KEY `idx_report_date` (`report_date`),
  KEY `idx_subcontractor` (`subcontractor`),
  KEY `idx_manpower` (`manpower`)
);

-- O&M Manuals (closeout)
CREATE TABLE em_om_manuals (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `spec_section` varchar(191) DEFAULT NULL,
  `subcontractor` varchar(191) DEFAULT NULL,
  `received_date` date DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_spec_section` (`spec_section`),
  KEY `idx_subcontractor` (`subcontractor`),
  KEY `idx_received_date` (`received_date`)
);

-- Warranties (closeout)
CREATE TABLE em_warranties (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `item` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `duration` varchar(191) DEFAULT NULL,
  `start_date` date DEFAULT NULL,
  `end_date` date DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  `terms` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_item` (`item`),
  KEY `idx_company` (`company`),
  KEY `idx_duration` (`duration`),
  KEY `idx_start_date` (`start_date`),
  KEY `idx_end_date` (`end_date`)
);

-- Attic Stock (closeout)
CREATE TABLE em_attic_stock (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `item` varchar(191) DEFAULT NULL,
  `spec_section` varchar(191) DEFAULT NULL,
  `quantity` varchar(191) DEFAULT NULL,
  `storage_location` varchar(191) DEFAULT NULL,
  `received_date` date DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_item` (`item`),
  KEY `idx_spec_section` (`spec_section`),
  KEY `idx_quantity` (`quantity`),
  KEY `idx_storage_location` (`storage_location`),
  KEY `idx_received_date` (`received_date`)
);

-- Locations (resources)
CREATE TABLE em_locations (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `name` varchar(191) DEFAULT NULL,
  `area` varchar(191) DEFAULT NULL,
  `level` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_name` (`name`),
  KEY `idx_area` (`area`),
  KEY `idx_level` (`level`)
);

-- CSI Divisions (resources)
CREATE TABLE em_csi_divisions (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `number` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_number` (`number`),
  KEY `idx_title` (`title`)
);

-- Cost Codes (resources)
CREATE TABLE em_cost_codes (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `code` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `division` varchar(191) DEFAULT NULL,
  `category` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_code` (`code`),
  KEY `idx_title` (`title`),
  KEY `idx_division` (`division`),
  KEY `idx_category` (`category`)
);

-- Labor Rates (resources)
CREATE TABLE em_labor_rates (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `classification` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `base_rate` decimal(20,4) DEFAULT NULL,
  `burden_rate` decimal(20,4) DEFAULT NULL,
  `overtime_rate` decimal(20,4) DEFAULT NULL,
  `effective_date` date DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_classification` (`classification`),
  KEY `idx_trade` (`trade`),
  KEY `idx_base_rate` (`base_rate`),
  KEY `idx_burden_rate` (`burden_rate`),
  KEY `idx_effective_date` (`effective_date`)
);

-- Material Rates (resources)
CREATE TABLE em_material_rates (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `material` varchar(191) DEFAULT NULL,
  `unit` varchar(191) DEFAULT NULL,
  `unit_cost` decimal(20,4) DEFAULT NULL,
  `supplier` varchar(191) DEFAULT NULL,
  `effective_date` date DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_material` (`material`),
  KEY `idx_unit` (`unit`),
  KEY `idx_unit_cost` (`unit_cost`),
  KEY `idx_supplier` (`supplier`),
  KEY `idx_effective_date` (`effective_date`)
);

-- Equipment Rates (resources)
CREATE TABLE em_equipment_rates (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `equipment` varchar(191) DEFAULT NULL,
  `rate_basis` varchar(191) DEFAULT NULL,
  `rate` decimal(20,4) DEFAULT NULL,
  `operated` tinyint(1) DEFAULT NULL,
  `vendor` varchar(191) DEFAULT NULL,
  `effective_date` date DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_equipment` (`equipment`),
  KEY `idx_rate_basis` (`rate_basis`),
  KEY `idx_rate` (`rate`),
  KEY `idx_vendor` (`vendor`)
);

-- Prequalification (preconstruction)
CREATE TABLE em_prequalification (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `company_name` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `contact_name` varchar(191) DEFAULT NULL,
  `emr` decimal(20,4) DEFAULT NULL,
  `annual_revenue` decimal(20,4) DEFAULT NULL,
  `bonding_capacity` decimal(20,4) DEFAULT NULL,
  `single_project_limit` decimal(20,4) DEFAULT NULL,
  `safety_score` decimal(20,4) DEFAULT NULL,
  `financial_score` decimal(20,4) DEFAULT NULL,
  `overall_score` decimal(20,4) DEFAULT NULL,
  `expiration_date` date DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_company_name` (`company_name`),
  KEY `idx_trade` (`trade`),
  KEY `idx_bonding_capacity` (`bonding_capacity`),
  KEY `idx_safety_score` (`safety_score`),
  KEY `idx_overall_score` (`overall_score`),
  KEY `idx_expiration_date` (`expiration_date`)
);

-- Bid Solicitations (ITB) (preconstruction)
CREATE TABLE em_bid_solicitations (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `solicitation_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `bid_package` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `issue_date` date DEFAULT NULL,
  `walkthrough_date` datetime DEFAULT NULL,
  `due_date` date DEFAULT NULL,
  `estimate` decimal(20,4) DEFAULT NULL,
  `scope` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_solicitation_no` (`solicitation_no`),
  KEY `idx_title` (`title`),
  KEY `idx_bid_package` (`bid_package`),
  KEY `idx_issue_date` (`issue_date`),
  KEY `idx_due_date` (`due_date`),
  KEY `idx_estimate` (`estimate`)
);

-- Bid Submissions (Leveling) (preconstruction)
CREATE TABLE em_bid_submissions (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `bidder` varchar(191) DEFAULT NULL,
  `bid_package` varchar(191) DEFAULT NULL,
  `base_bid` decimal(20,4) DEFAULT NULL,
  `alternates_total` decimal(20,4) DEFAULT NULL,
  `submitted_date` date DEFAULT NULL,
  `inclusions` text DEFAULT NULL,
  `exclusions` text DEFAULT NULL,
  `score` decimal(20,4) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_bidder` (`bidder`),
  KEY `idx_bid_package` (`bid_package`),
  KEY `idx_base_bid` (`base_bid`),
  KEY `idx_alternates_total` (`alternates_total`),
  KEY `idx_submitted_date` (`submitted_date`),
  KEY `idx_score` (`score`)
);

-- Estimates (preconstruction)
CREATE TABLE em_estimates (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `estimate_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `csi_division` varchar(191) DEFAULT NULL,
  `quantity` decimal(20,4) DEFAULT NULL,
  `unit` varchar(191) DEFAULT NULL,
  `unit_cost` decimal(20,4) DEFAULT NULL,
  `total` decimal(20,4) DEFAULT NULL,
  `basis` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_estimate_no` (`estimate_no`),
  KEY `idx_title` (`title`),
  KEY `idx_csi_division` (`csi_division`),
  KEY `idx_total` (`total`)
);

-- Value Engineering (preconstruction)
CREATE TABLE em_value_engineering (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `ve_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `proposed_savings` decimal(20,4) DEFAULT NULL,
  `schedule_impact` varchar(191) DEFAULT NULL,
  `quality_impact` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_ve_no` (`ve_no`),
  KEY `idx_title` (`title`),
  KEY `idx_proposed_savings` (`proposed_savings`)
);

-- Issues (engineering)
CREATE TABLE em_issues (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `issue_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `issue_type` varchar(191) DEFAULT NULL,
  `priority` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `assigned_to` varchar(191) DEFAULT NULL,
  `due_date` date DEFAULT NULL,
  `description` text DEFAULT NULL,
  `resolution` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_issue_no` (`issue_no`),
  KEY `idx_title` (`title`),
  KEY `idx_issue_type` (`issue_type`),
  KEY `idx_priority` (`priority`),
  KEY `idx_assigned_to` (`assigned_to`),
  KEY `idx_due_date` (`due_date`)
);

-- Action Items (engineering)
CREATE TABLE em_action_items (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `assigned_to` varchar(191) DEFAULT NULL,
  `priority` varchar(191) DEFAULT NULL,
  `category` varchar(191) DEFAULT NULL,
  `due_date` date DEFAULT NULL,
  `source` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_assigned_to` (`assigned_to`),
  KEY `idx_priority` (`priority`),
  KEY `idx_category` (`category`),
  KEY `idx_due_date` (`due_date`)
);

-- Correspondence (engineering)
CREATE TABLE em_correspondence (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `ref_no` varchar(191) DEFAULT NULL,
  `subject` varchar(191) DEFAULT NULL,
  `corr_type` varchar(191) DEFAULT NULL,
  `flow` varchar(191) DEFAULT NULL,
  `from_party` varchar(191) DEFAULT NULL,
  `to_party` varchar(191) DEFAULT NULL,
  `date_sent` date DEFAULT NULL,
  `body` text DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_ref_no` (`ref_no`),
  KEY `idx_subject` (`subject`),
  KEY `idx_corr_type` (`corr_type`),
  KEY `idx_flow` (`flow`),
  KEY `idx_from_party` (`from_party`),
  KEY `idx_to_party` (`to_party`),
  KEY `idx_date_sent` (`date_sent`)
);

-- Design Reviews (engineering)
CREATE TABLE em_design_reviews (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `review_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `discipline` varchar(191) DEFAULT NULL,
  `phase` varchar(191) DEFAULT NULL,
  `reviewer` varchar(191) DEFAULT NULL,
  `review_date` date DEFAULT NULL,
  `comments` text DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_review_no` (`review_no`),
  KEY `idx_title` (`title`),
  KEY `idx_discipline` (`discipline`),
  KEY `idx_phase` (`phase`),
  KEY `idx_reviewer` (`reviewer`),
  KEY `idx_review_date` (`review_date`)
);

-- Inspections (quality)
CREATE TABLE em_inspections (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `inspection_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `inspection_type` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `inspector` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `scheduled_date` date DEFAULT NULL,
  `completed_date` date DEFAULT NULL,
  `checklist` text DEFAULT NULL,
  `findings` text DEFAULT NULL,
  `inspector_signature` mediumtext DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_inspection_no` (`inspection_no`),
  KEY `idx_title` (`title`),
  KEY `idx_inspection_type` (`inspection_type`),
  KEY `idx_location` (`location`),
  KEY `idx_inspector` (`inspector`),
  KEY `idx_trade` (`trade`),
  KEY `idx_scheduled_date` (`scheduled_date`),
  KEY `idx_completed_date` (`completed_date`)
);

-- Non-Conformance Reports (quality)
CREATE TABLE em_non_conformance (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `ncr_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `spec_section` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `responsible` varchar(191) DEFAULT NULL,
  `severity` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `root_cause` text DEFAULT NULL,
  `corrective_action` text DEFAULT NULL,
  `preventive_action` text DEFAULT NULL,
  `due_date` date DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_ncr_no` (`ncr_no`),
  KEY `idx_title` (`title`),
  KEY `idx_spec_section` (`spec_section`),
  KEY `idx_location` (`location`),
  KEY `idx_responsible` (`responsible`),
  KEY `idx_severity` (`severity`),
  KEY `idx_due_date` (`due_date`)
);

-- Deficiencies / Defects (quality)
CREATE TABLE em_deficiencies (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `defect_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `ball_in_court` varchar(191) DEFAULT NULL,
  `severity` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `due_date` date DEFAULT NULL,
  `photo_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_defect_no` (`defect_no`),
  KEY `idx_title` (`title`),
  KEY `idx_location` (`location`),
  KEY `idx_trade` (`trade`),
  KEY `idx_ball_in_court` (`ball_in_court`),
  KEY `idx_severity` (`severity`),
  KEY `idx_due_date` (`due_date`)
);

-- Test Records (quality)
CREATE TABLE em_test_records (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `test_no` varchar(191) DEFAULT NULL,
  `test_type` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `test_date` date DEFAULT NULL,
  `reading_value` varchar(191) DEFAULT NULL,
  `spec_requirement` varchar(191) DEFAULT NULL,
  `lab` varchar(191) DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_test_no` (`test_no`),
  KEY `idx_test_type` (`test_type`),
  KEY `idx_test_date` (`test_date`),
  KEY `idx_lab` (`lab`)
);

-- Incidents (safety)
CREATE TABLE em_incidents (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `incident_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `incident_type` varchar(191) DEFAULT NULL,
  `severity` varchar(191) DEFAULT NULL,
  `incident_date` datetime DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `company_involved` varchar(191) DEFAULT NULL,
  `injured_party` varchar(191) DEFAULT NULL,
  `body_part` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  `root_cause_method` varchar(191) DEFAULT NULL,
  `root_cause` text DEFAULT NULL,
  `corrective_action` text DEFAULT NULL,
  `lost_days` decimal(20,4) DEFAULT NULL,
  `restricted_days` decimal(20,4) DEFAULT NULL,
  `osha_recordable` tinyint(1) DEFAULT NULL,
  `reported_to_osha` tinyint(1) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_incident_no` (`incident_no`),
  KEY `idx_title` (`title`),
  KEY `idx_incident_type` (`incident_type`),
  KEY `idx_severity` (`severity`),
  KEY `idx_incident_date` (`incident_date`),
  KEY `idx_company_involved` (`company_involved`)
);

-- Safety Violations (safety)
CREATE TABLE em_safety_violations (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `violation_no` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `violation_type` varchar(191) DEFAULT NULL,
  `severity` varchar(191) DEFAULT NULL,
  `observed_date` date DEFAULT NULL,
  `description` text DEFAULT NULL,
  `corrective_action` text DEFAULT NULL,
  `fine` decimal(20,4) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_violation_no` (`violation_no`),
  KEY `idx_company` (`company`),
  KEY `idx_violation_type` (`violation_type`),
  KEY `idx_severity` (`severity`),
  KEY `idx_observed_date` (`observed_date`)
);

-- Toolbox Talks (safety)
CREATE TABLE em_toolbox_talks (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `topic` varchar(191) DEFAULT NULL,
  `talk_date` date DEFAULT NULL,
  `presenter` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `attendee_count` decimal(20,4) DEFAULT NULL,
  `attendees` text DEFAULT NULL,
  `notes` text DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_topic` (`topic`),
  KEY `idx_talk_date` (`talk_date`),
  KEY `idx_presenter` (`presenter`),
  KEY `idx_company` (`company`),
  KEY `idx_attendee_count` (`attendee_count`)
);

-- Timesheets (field)
CREATE TABLE em_timesheets (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `work_date` date DEFAULT NULL,
  `worker_name` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `cost_code` varchar(191) DEFAULT NULL,
  `regular_hours` decimal(20,4) DEFAULT NULL,
  `overtime_hours` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_work_date` (`work_date`),
  KEY `idx_worker_name` (`worker_name`),
  KEY `idx_company` (`company`),
  KEY `idx_trade` (`trade`),
  KEY `idx_regular_hours` (`regular_hours`),
  KEY `idx_overtime_hours` (`overtime_hours`)
);

-- Crews (field)
CREATE TABLE em_crews (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `crew_name` varchar(191) DEFAULT NULL,
  `foreman` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `headcount` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_crew_name` (`crew_name`),
  KEY `idx_foreman` (`foreman`),
  KEY `idx_company` (`company`),
  KEY `idx_trade` (`trade`),
  KEY `idx_headcount` (`headcount`)
);

-- Production Quantities (field)
CREATE TABLE em_production_quantities (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `activity` varchar(191) DEFAULT NULL,
  `cost_code` varchar(191) DEFAULT NULL,
  `unit` varchar(191) DEFAULT NULL,
  `planned_qty` decimal(20,4) DEFAULT NULL,
  `installed_qty` decimal(20,4) DEFAULT NULL,
  `work_date` date DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_activity` (`activity`),
  KEY `idx_cost_code` (`cost_code`),
  KEY `idx_installed_qty` (`installed_qty`),
  KEY `idx_work_date` (`work_date`)
);

-- Manpower Log (field)
CREATE TABLE em_manpower_log (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `log_date` date DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `workers` decimal(20,4) DEFAULT NULL,
  `hours` decimal(20,4) DEFAULT NULL,
  `area` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_log_date` (`log_date`),
  KEY `idx_company` (`company`),
  KEY `idx_trade` (`trade`),
  KEY `idx_workers` (`workers`),
  KEY `idx_hours` (`hours`)
);

-- Deliveries (field)
CREATE TABLE em_deliveries (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `delivery_date` date DEFAULT NULL,
  `supplier` varchar(191) DEFAULT NULL,
  `material` varchar(191) DEFAULT NULL,
  `po_number` varchar(191) DEFAULT NULL,
  `quantity` varchar(191) DEFAULT NULL,
  `received_by` varchar(191) DEFAULT NULL,
  `item_condition` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_delivery_date` (`delivery_date`),
  KEY `idx_supplier` (`supplier`),
  KEY `idx_material` (`material`),
  KEY `idx_po_number` (`po_number`)
);

-- Visitor Log (field)
CREATE TABLE em_visitor_log (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `visit_date` date DEFAULT NULL,
  `visitor_name` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `purpose` varchar(191) DEFAULT NULL,
  `time_in` varchar(191) DEFAULT NULL,
  `time_out` varchar(191) DEFAULT NULL,
  `escort` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_visit_date` (`visit_date`),
  KEY `idx_visitor_name` (`visitor_name`),
  KEY `idx_company` (`company`),
  KEY `idx_purpose` (`purpose`)
);

-- Equipment Log (field)
CREATE TABLE em_equipment_log (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `equipment` varchar(191) DEFAULT NULL,
  `equipment_id` varchar(191) DEFAULT NULL,
  `supplier` varchar(191) DEFAULT NULL,
  `arrival_date` date DEFAULT NULL,
  `hours_used` decimal(20,4) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_equipment` (`equipment`),
  KEY `idx_equipment_id` (`equipment_id`),
  KEY `idx_supplier` (`supplier`),
  KEY `idx_arrival_date` (`arrival_date`)
);

-- Commitments (POs) (cost)
CREATE TABLE em_commitments (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `commitment_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `commitment_type` varchar(191) DEFAULT NULL,
  `vendor` varchar(191) DEFAULT NULL,
  `csi_division` varchar(191) DEFAULT NULL,
  `cost_code` varchar(191) DEFAULT NULL,
  `value` decimal(20,4) DEFAULT NULL,
  `executed_date` date DEFAULT NULL,
  `retainage_pct` decimal(20,4) DEFAULT NULL,
  `scope` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_commitment_no` (`commitment_no`),
  KEY `idx_title` (`title`),
  KEY `idx_commitment_type` (`commitment_type`),
  KEY `idx_vendor` (`vendor`),
  KEY `idx_cost_code` (`cost_code`),
  KEY `idx_value` (`value`),
  KEY `idx_executed_date` (`executed_date`)
);

-- Change Events (cost)
CREATE TABLE em_change_events (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `event_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `event_type` varchar(191) DEFAULT NULL,
  `rom_cost` decimal(20,4) DEFAULT NULL,
  `rom_schedule_days` decimal(20,4) DEFAULT NULL,
  `cost_code` varchar(191) DEFAULT NULL,
  `description` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_event_no` (`event_no`),
  KEY `idx_title` (`title`),
  KEY `idx_event_type` (`event_type`),
  KEY `idx_rom_cost` (`rom_cost`)
);

-- Owner Invoices (Requisitions) (cost)
CREATE TABLE em_owner_invoices (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `invoice_no` varchar(191) DEFAULT NULL,
  `period_to` date DEFAULT NULL,
  `contract_value` decimal(20,4) DEFAULT NULL,
  `work_completed` decimal(20,4) DEFAULT NULL,
  `stored_materials` decimal(20,4) DEFAULT NULL,
  `retainage` decimal(20,4) DEFAULT NULL,
  `amount_due` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_invoice_no` (`invoice_no`),
  KEY `idx_period_to` (`period_to`),
  KEY `idx_work_completed` (`work_completed`),
  KEY `idx_amount_due` (`amount_due`)
);

-- Subcontractor Invoices (cost)
CREATE TABLE em_subcontractor_invoices (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `invoice_no` varchar(191) DEFAULT NULL,
  `subcontractor` varchar(191) DEFAULT NULL,
  `commitment_no` varchar(191) DEFAULT NULL,
  `period_to` date DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `retainage` decimal(20,4) DEFAULT NULL,
  `approved_amount` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_invoice_no` (`invoice_no`),
  KEY `idx_subcontractor` (`subcontractor`),
  KEY `idx_commitment_no` (`commitment_no`),
  KEY `idx_period_to` (`period_to`),
  KEY `idx_amount` (`amount`),
  KEY `idx_approved_amount` (`approved_amount`)
);

-- Funding Sources (cost)
CREATE TABLE em_funding_sources (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `source_name` varchar(191) DEFAULT NULL,
  `funding_type` varchar(191) DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `reference` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_source_name` (`source_name`),
  KEY `idx_funding_type` (`funding_type`),
  KEY `idx_amount` (`amount`)
);

-- Allowances (cost)
CREATE TABLE em_allowances (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `allowance_no` varchar(191) DEFAULT NULL,
  `description` varchar(191) DEFAULT NULL,
  `budgeted` decimal(20,4) DEFAULT NULL,
  `used` decimal(20,4) DEFAULT NULL,
  `remaining` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_allowance_no` (`allowance_no`),
  KEY `idx_description` (`description`),
  KEY `idx_budgeted` (`budgeted`),
  KEY `idx_used` (`used`),
  KEY `idx_remaining` (`remaining`)
);

-- Contingency Log (cost)
CREATE TABLE em_contingency_log (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `entry_no` varchar(191) DEFAULT NULL,
  `description` varchar(191) DEFAULT NULL,
  `contingency_type` varchar(191) DEFAULT NULL,
  `amount` decimal(20,4) DEFAULT NULL,
  `entry_date` date DEFAULT NULL,
  `reference` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_entry_no` (`entry_no`),
  KEY `idx_description` (`description`),
  KEY `idx_contingency_type` (`contingency_type`),
  KEY `idx_amount` (`amount`),
  KEY `idx_entry_date` (`entry_date`)
);

-- LEED / Green Credits (sustainability)
CREATE TABLE em_leed_credits (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `credit_id` varchar(191) DEFAULT NULL,
  `credit_name` varchar(191) DEFAULT NULL,
  `category` varchar(191) DEFAULT NULL,
  `points_targeted` decimal(20,4) DEFAULT NULL,
  `points_achieved` decimal(20,4) DEFAULT NULL,
  `responsible` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_credit_id` (`credit_id`),
  KEY `idx_credit_name` (`credit_name`),
  KEY `idx_category` (`category`),
  KEY `idx_points_targeted` (`points_targeted`),
  KEY `idx_points_achieved` (`points_achieved`),
  KEY `idx_responsible` (`responsible`)
);

-- Waste Diversion (sustainability)
CREATE TABLE em_waste_diversion (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `log_date` date DEFAULT NULL,
  `material` varchar(191) DEFAULT NULL,
  `weight_tons` decimal(20,4) DEFAULT NULL,
  `disposal_method` varchar(191) DEFAULT NULL,
  `hauler` varchar(191) DEFAULT NULL,
  `ticket_no` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_log_date` (`log_date`),
  KEY `idx_material` (`material`),
  KEY `idx_weight_tons` (`weight_tons`),
  KEY `idx_disposal_method` (`disposal_method`),
  KEY `idx_hauler` (`hauler`)
);

-- Environmental Monitoring (sustainability)
CREATE TABLE em_environmental_monitoring (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `monitor_date` date DEFAULT NULL,
  `parameter` varchar(191) DEFAULT NULL,
  `reading` varchar(191) DEFAULT NULL,
  `permit_limit` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `action_taken` text DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_monitor_date` (`monitor_date`),
  KEY `idx_parameter` (`parameter`),
  KEY `idx_reading` (`reading`)
);

-- Commissioning (Cx) (closeout)
CREATE TABLE em_commissioning (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `system` varchar(191) DEFAULT NULL,
  `equipment_tag` varchar(191) DEFAULT NULL,
  `cx_phase` varchar(191) DEFAULT NULL,
  `scheduled_date` date DEFAULT NULL,
  `responsible` varchar(191) DEFAULT NULL,
  `findings` text DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_system` (`system`),
  KEY `idx_equipment_tag` (`equipment_tag`),
  KEY `idx_cx_phase` (`cx_phase`),
  KEY `idx_scheduled_date` (`scheduled_date`),
  KEY `idx_responsible` (`responsible`)
);

-- As-Builts / Record Drawings (closeout)
CREATE TABLE em_as_builts (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `drawing_no` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `discipline` varchar(191) DEFAULT NULL,
  `trade` varchar(191) DEFAULT NULL,
  `submitted_date` date DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_drawing_no` (`drawing_no`),
  KEY `idx_title` (`title`),
  KEY `idx_discipline` (`discipline`),
  KEY `idx_trade` (`trade`),
  KEY `idx_submitted_date` (`submitted_date`)
);

-- Owner Training (closeout)
CREATE TABLE em_training_records (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `system` varchar(191) DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `training_date` date DEFAULT NULL,
  `trainer` varchar(191) DEFAULT NULL,
  `attendee_count` decimal(20,4) DEFAULT NULL,
  `file_url` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_system` (`system`),
  KEY `idx_title` (`title`),
  KEY `idx_training_date` (`training_date`),
  KEY `idx_trainer` (`trainer`)
);

-- Completion Certificates (closeout)
CREATE TABLE em_completion_certificates (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `cert_no` varchar(191) DEFAULT NULL,
  `cert_type` varchar(191) DEFAULT NULL,
  `description` varchar(191) DEFAULT NULL,
  `issue_date` date DEFAULT NULL,
  `value_remaining` decimal(20,4) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_cert_no` (`cert_no`),
  KEY `idx_cert_type` (`cert_type`),
  KEY `idx_description` (`description`),
  KEY `idx_issue_date` (`issue_date`)
);

-- Asset Register (closeout)
CREATE TABLE em_asset_register (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `asset_tag` varchar(191) DEFAULT NULL,
  `asset_name` varchar(191) DEFAULT NULL,
  `system` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `manufacturer` varchar(191) DEFAULT NULL,
  `model` varchar(191) DEFAULT NULL,
  `serial` varchar(191) DEFAULT NULL,
  `install_date` date DEFAULT NULL,
  `warranty_end` date DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_asset_tag` (`asset_tag`),
  KEY `idx_asset_name` (`asset_name`),
  KEY `idx_system` (`system`),
  KEY `idx_location` (`location`),
  KEY `idx_manufacturer` (`manufacturer`),
  KEY `idx_warranty_end` (`warranty_end`)
);

-- Linear / Takt Schedule (field)
CREATE TABLE em_linear_schedule (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `activity` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `location_index` decimal(20,4) DEFAULT NULL,
  `crew` varchar(191) DEFAULT NULL,
  `takt_zone` varchar(191) DEFAULT NULL,
  `planned_start` date DEFAULT NULL,
  `planned_finish` date DEFAULT NULL,
  `duration_days` decimal(20,4) DEFAULT NULL,
  `production_rate` varchar(191) DEFAULT NULL,
  `predecessor` varchar(191) DEFAULT NULL,
  `percent_complete` decimal(20,4) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_activity` (`activity`),
  KEY `idx_location` (`location`),
  KEY `idx_location_index` (`location_index`),
  KEY `idx_crew` (`crew`),
  KEY `idx_planned_start` (`planned_start`),
  KEY `idx_planned_finish` (`planned_finish`),
  KEY `idx_percent_complete` (`percent_complete`)
);

-- Site Logistics (field)
CREATE TABLE em_site_logistics (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `logistics_date` date DEFAULT NULL,
  `resource` varchar(191) DEFAULT NULL,
  `activity` varchar(191) DEFAULT NULL,
  `company` varchar(191) DEFAULT NULL,
  `time_window` varchar(191) DEFAULT NULL,
  `location` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_logistics_date` (`logistics_date`),
  KEY `idx_resource` (`resource`),
  KEY `idx_activity` (`activity`),
  KEY `idx_company` (`company`),
  KEY `idx_time_window` (`time_window`)
);

-- Procurement Log (Long-Lead) (engineering)
CREATE TABLE em_procurement_log (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `item` varchar(191) DEFAULT NULL,
  `csi_division` varchar(191) DEFAULT NULL,
  `supplier` varchar(191) DEFAULT NULL,
  `lead_time_weeks` decimal(20,4) DEFAULT NULL,
  `order_by` date DEFAULT NULL,
  `need_by` date DEFAULT NULL,
  `submittal_ref` varchar(191) DEFAULT NULL,
  `notes` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_item` (`item`),
  KEY `idx_csi_division` (`csi_division`),
  KEY `idx_supplier` (`supplier`),
  KEY `idx_lead_time_weeks` (`lead_time_weeks`),
  KEY `idx_order_by` (`order_by`),
  KEY `idx_need_by` (`need_by`)
);

-- Schedule of Values (G703) (cost)
CREATE TABLE em_schedule_of_values (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `item_no` varchar(191) DEFAULT NULL,
  `description` varchar(191) DEFAULT NULL,
  `scheduled_value` decimal(20,4) DEFAULT NULL,
  `previous_completed` decimal(20,4) DEFAULT NULL,
  `this_period` decimal(20,4) DEFAULT NULL,
  `materials_stored` decimal(20,4) DEFAULT NULL,
  `total_completed` decimal(20,4) DEFAULT NULL,
  `percent_complete` decimal(20,4) DEFAULT NULL,
  `balance_to_finish` decimal(20,4) DEFAULT NULL,
  `retainage` decimal(20,4) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_item_no` (`item_no`),
  KEY `idx_description` (`description`),
  KEY `idx_scheduled_value` (`scheduled_value`),
  KEY `idx_total_completed` (`total_completed`),
  KEY `idx_percent_complete` (`percent_complete`),
  KEY `idx_balance_to_finish` (`balance_to_finish`)
);

-- Company Management (settings)
CREATE TABLE em_companies (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `name` varchar(191) DEFAULT NULL,
  `type` varchar(191) DEFAULT NULL,
  `phone` varchar(191) DEFAULT NULL,
  `email` varchar(191) DEFAULT NULL,
  `address` varchar(191) DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_name` (`name`),
  KEY `idx_type` (`type`),
  KEY `idx_phone` (`phone`),
  KEY `idx_email` (`email`)
);

-- Project Help (settings)
CREATE TABLE em_project_help (
  id bigint(20) unsigned NOT NULL auto_increment,
  project_id varchar(191) DEFAULT NULL,
  company_id varchar(191) DEFAULT NULL,
  status varchar(191) DEFAULT NULL,
  direction varchar(32) DEFAULT NULL,
  linked_module varchar(191) DEFAULT NULL,
  linked_id bigint(20) unsigned DEFAULT NULL,
  created_by varchar(20) DEFAULT NULL,
  created_by_name varchar(191) DEFAULT NULL,
  created_at datetime DEFAULT NULL,
  updated_at datetime DEFAULT NULL,
  `title` varchar(191) DEFAULT NULL,
  `category` varchar(191) DEFAULT NULL,
  `body` text DEFAULT NULL,
  PRIMARY KEY  (id),
  KEY status (status),
  KEY created_at (created_at),
  KEY created_by (created_by),
  KEY project_status (project_id,status),
  KEY linked (linked_module,linked_id),
  KEY `idx_title` (`title`),
  KEY `idx_category` (`category`)
);
